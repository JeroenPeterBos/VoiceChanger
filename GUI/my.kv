<RootWidget>:
	orientation: "vertical"
		Slider:
			min: 0
			max: 100
			value: 50
			on_value: color_label.text = str(round(self.value))+"%"; root.sliders_colors = (self.value for i in xrange(3))
		
